<?xml version="1.0" encoding="utf-8"?>
<!--
  Adaptive icon (Android 8.0+). Launchers apply their own masks, parallax, and
  themed-icon treatment, so the approved artwork is inset into the adaptive
  foreground safe zone instead of being placed edge-to-edge.
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@drawable/ic_launcher_background" />
    <foreground android:drawable="@drawable/ic_launcher_foreground_inset" />
    <monochrome android:drawable="@drawable/ic_launcher_monochrome" />
</adaptive-icon>
